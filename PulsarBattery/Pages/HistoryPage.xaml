<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PulsarBattery.Pages.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:PulsarBattery.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid Padding="32">
            <Grid HorizontalAlignment="Stretch">
                <StackPanel Spacing="12">

                    <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                          BorderThickness="1"
                          Padding="12"
                          CornerRadius="12">

                        <Grid>
                            <ListView ItemsSource="{Binding History}"
                                      SelectionMode="None"
                                      IsItemClickEnabled="False">
                                <ListView.Header>
                                    <Grid Padding="0,0,0,10" ColumnSpacing="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="2*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Time" Grid.Column="0" Style="{ThemeResource BodyStrongTextBlockStyle}" Opacity="0.9" />
                                        <TextBlock Text="Battery" Grid.Column="1" Style="{ThemeResource BodyStrongTextBlockStyle}" Opacity="0.9" />
                                        <TextBlock Text="Charging" Grid.Column="2" Style="{ThemeResource BodyStrongTextBlockStyle}" Opacity="0.9" />
                                        <TextBlock Text="Model" Grid.Column="3" Style="{ThemeResource BodyStrongTextBlockStyle}" Opacity="0.9" />
                                    </Grid>
                                </ListView.Header>
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="models:BatteryReading">
                                        <Grid Padding="0,20,0,0" ColumnSpacing="0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="2*" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="2*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{x:Bind FormattedTimestamp}" />
                                            <TextBlock Grid.Column="1"><Run Text="{x:Bind Percentage}" /><Run Text="%" /></TextBlock>
                                            <TextBlock Grid.Column="2" Text="{x:Bind ChargingStatus}" />
                                            <TextBlock Grid.Column="3" Text="{x:Bind Model}" TextTrimming="CharacterEllipsis" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>

                            <StackPanel HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Spacing="8"
                                        Visibility="{Binding HistoryEmptyVisibility}">
                                <FontIcon Glyph="&#xE81C;" Opacity="0.7" FontSize="32" />
                                <TextBlock Text="No history yet" Style="{ThemeResource SubtitleTextBlockStyle}" HorizontalAlignment="Center" />
                                <TextBlock Text="Battery readings will appear here over time." Opacity="0.7" TextAlignment="Center" TextWrapping="Wrap" MaxWidth="360" />
                            </StackPanel>
                        </Grid>
                    </Grid>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
